- category ||= edit

= form_for @category, :as => :category, :url => admins_cms_site_category_path(@site, category), :remote => true, :html => {:id => dom_id(category)} do |form|
  .input-append
    = form.text_field :label
    %button.btn.btn-mini= t('.save')