.page-header
  = link_to t('.new_link'), new_admins_cms_site_page_path(@site), :class => 'btn pull-right'
  %h2= t('.title')

- content_for :right_column do
  = render :partial => 'admins/cms/sites/mirrors'

= render 'admins/cms/partials/pages_before'

= render :partial => 'admins/cms/categories/index', :object => 'Cms::Page'

%ul.list.sortable
  = render :partial => 'index_branch', :collection => @pages

= render 'admins/cms/partials/pages_after'